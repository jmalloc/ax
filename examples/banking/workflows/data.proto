syntax = "proto3";

package ax.examples.banking.workflows;
option go_package = "workflows";

message Transfer {
    string transfer_id = 1;
    string from_account_id = 2;
    string to_account_id = 3;
    int32 amount_in_cents = 4;
    bool is_complete = 5;
}
